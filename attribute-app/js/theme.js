import { getTheme, saveTheme } from './storage.js'; export function applyTheme(theme) { const resolved = theme === 'dark' ? 'dark' : 'light'; document.documentElement.setAttribute('data-theme', resolved); document.documentElement.style.colorScheme = resolved; const toggle = document.getElementById('theme-toggle'); if (toggle) { toggle.textContent = resolved === 'dark' ? 'Light mode' : 'Dark mode'; toggle.setAttribute('aria-pressed', resolved === 'dark' ? 'true' : 'false'); } return resolved; }
export function toggleTheme() { const resolved = document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark'; saveTheme(resolved); return applyTheme(resolved); }
export function initThemeToggle(toggle) { if (!toggle) return; toggle.addEventListener('click', () => toggleTheme()); applyTheme(getTheme()); }
