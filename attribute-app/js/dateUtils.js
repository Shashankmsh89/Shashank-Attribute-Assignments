const dateFormatter = new Intl.DateTimeFormat('en', { month: 'short', day: 'numeric', year: 'numeric' }); const relativeFormatter = new Intl.RelativeTimeFormat('en', { numeric: 'auto' }); export function formatDate(value) { const date = new Date(value); if (Number.isNaN(date.getTime())) return 'Unknown'; return dateFormatter.format(date); } export function formatRelativeTime(value) { const date = new Date(value); if (Number.isNaN(date.getTime())) return 'Unknown'; const delta = date.getTime() - Date.now(); const days = Math.round(delta / (1000 * 60 * 60 * 24)); return relativeFormatter.format(days, 'day'); } export function getDefaultDate() { return new Date().toISOString().split('T')[0]; }
