export function validateAttributeForm(values) { const errors = {}; const attributeName = String(values.attributeName || '').trim(); if (!attributeName) errors.attributeName = 'Attribute name is required.'; else if (attributeName.length < 3) errors.attributeName = 'Attribute name must be at least 3 characters.'; else if (!/^[A-Za-z0-9 _-]+$/.test(attributeName)) errors.attributeName = 'Only letters, numbers, spaces, _ and - are allowed.'; if (!values.businessUnit) errors.businessUnit = 'Choose a business unit.'; if (!values.customerLocation) errors.customerLocation = 'Choose a customer location.'; if (!values.company) errors.company = 'Choose a company.'; if (!values.createdOn) errors.createdOn = 'Choose a creation date.'; const notes = String(values.notes || '').trim(); if (notes.length > 500) errors.notes = 'Notes must stay below 500 characters.'; return errors; }
export function getErrorSummary(errors) { return Object.entries(errors).map(([field, message]) => ({ field, message })); }
